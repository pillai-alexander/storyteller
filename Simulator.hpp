#pragma once

#include <vector>
#include <memory>
#include "Community.hpp"
#include "Parameters.hpp"

class Simulator {
  public:
    Simulator();
    ~Simulator();

  private:
    // community object
    // parameters object
};