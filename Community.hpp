#pragma once

#include <vector>
#include "Person.hpp"

class Community {
 public:
    Community();
    ~Community();

 private:
    // vector of all people
    // vector of people still susceptible
};